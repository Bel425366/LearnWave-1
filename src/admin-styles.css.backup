/* Estilos para o Painel Administrativo */
.admin-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
  background: #1a1a2e;
  min-height: 100vh;
}

.admin-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(255, 255, 255, 0.1);
  padding: 20px;
  border-radius: 15px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  margin-bottom: 20px;
}

.admin-header h1 {
  margin: 0;
  color: #ffffff;
  font-size: 24px;
}

.admin-user-info {
  display: flex;
  align-items: center;
  gap: 15px;
}

.admin-user-info span {
  font-weight: 600;
  color: #ffffff;
}

.logout-btn {
  background: #ff4757;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: 600;
}

.logout-btn:hover {
  background: #ff3742;
}

.admin-tabs {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
}

.tab-btn {
  background: rgba(255,255,255,0.1);
  color: #ffffff;
  border: 2px solid rgba(255,255,255,0.2);
  padding: 12px 24px;
  border-radius: 10px;
  cursor: pointer;
  font-weight: 600;
  transition: all 0.3s ease;
}

.tab-btn:hover {
  background: rgba(255,255,255,0.3);
}

.tab-btn.active {
  background: rgba(255,255,255,0.2);
  color: #ffffff;
  border-color: rgba(255,255,255,0.4);
}

.admin-content {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 15px;
  padding: 30px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

/* Seção de Usuários */
.usuarios-section h2,
.config-section h2,
.relatorios-section h2 {
  margin: 0 0 25px 0;
  color: #ffffff;
  font-size: 22px;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25px;
}

.refresh-btn {
  background: #5dade2;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: 600;
}

.usuarios-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 20px;
}

.usuario-card {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  transition: transform 0.2s ease;
}

.usuario-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}

.usuario-avatar {
  font-size: 40px;
  text-align: center;
  margin-bottom: 15px;
}

.usuario-info h3 {
  margin: 0 0 8px 0;
  color: #ffffff;
  font-size: 18px;
}

.usuario-info .email {
  color: #b0b0b0;
  margin: 0 0 10px 0;
  font-size: 14px;
}

.tipo-badge {
  display: inline-block;
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
}

.tipo-aluno { background: #e3f2fd; color: #1976d2; }
.tipo-professor { background: #f3e5f5; color: #7b1fa2; }
.tipo-administrador { background: #fff3e0; color: #f57c00; }

.usuario-info .data {
  color: #b0b0b0;
  font-size: 12px;
  margin: 10px 0 0 0;
}

.usuario-actions {
  display: flex;
  gap: 10px;
  margin-top: 15px;
}

.edit-btn, .delete-btn {
  flex: 1;
  padding: 8px 12px;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 12px;
  font-weight: 600;
}

.edit-btn {
  background: #4caf50;
  color: white;
}

.delete-btn {
  background: #f44336;
  color: white;
}

/* Seção de Configurações */
.config-card {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 25px;
  margin-bottom: 20px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.config-card h3 {
  margin: 0 0 20px 0;
  color: #ffffff;
  font-size: 18px;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  color: #ffffff;
}

.config-input, .config-textarea {
  width: 100%;
  padding: 12px;
  border: 2px solid rgba(255, 255, 255, 0.2);
  border-radius: 8px;
  font-size: 14px;
  background: rgba(255, 255, 255, 0.1);
  color: #ffffff;
}

.config-textarea {
  height: 80px;
  resize: vertical;
}

.checkbox-group {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.checkbox-label {
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
}

.checkbox-label input {
  width: 18px;
  height: 18px;
}

.save-btn {
  background: #4caf50;
  color: white;
  border: none;
  padding: 12px 30px;
  border-radius: 8px;
  cursor: pointer;
  font-weight: 600;
  font-size: 16px;
}

/* Seção de Relatórios */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  margin-bottom: 30px;
}

.stat-card {
  background: rgba(255, 255, 255, 0.1);
  color: white;
  padding: 25px;
  border-radius: 15px;
  display: flex;
  align-items: center;
  gap: 15px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.stat-card.total { background: rgba(255, 255, 255, 0.15); }
.stat-card.alunos { background: rgba(255, 255, 255, 0.15); }
.stat-card.professores { background: rgba(255, 255, 255, 0.15); }
.stat-card.admins { background: rgba(255, 255, 255, 0.15); }

.stat-icon {
  font-size: 40px;
}

.stat-info h3 {
  margin: 0;
  font-size: 32px;
  font-weight: 700;
}

.stat-info p {
  margin: 5px 0 0 0;
  opacity: 0.9;
  font-size: 14px;
}

.activity-summary {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 25px;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.activity-summary h3 {
  margin: 0 0 20px 0;
  color: #ffffff;
}

.activity-stats {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.activity-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.2);
  color: #ffffff;
}

.activity-item:last-child {
  border-bottom: none;
}

.activity-item strong {
  color: #ff6b9d;
  font-size: 18px;
}

/* Verificação de Professores */
.verificacao-professores {
  padding: 20px;
}

.professores-lista {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.professor-card {
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 12px;
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
}

.professor-info h3 {
  margin: 0 0 10px 0;
  color: #ffffff;
}

.professor-info p {
  margin: 5px 0;
  color: #b0b0b0;
}

.professor-actions {
  display: flex;
  flex-direction: column;
  gap: 10px;
  align-items: flex-end;
}

.btn-documento {
  background: #007bff;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
}

.btn-documento:hover {
  background: #0056b3;
}

.verification-buttons {
  display: flex;
  gap: 10px;
}

.btn-aprovar {
  background: #28a745;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
}

.btn-aprovar:hover {
  background: #1e7e34;
}

.btn-rejeitar {
  background: #dc3545;
  color: white;
  border: none;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
}

.btn-rejeitar:hover {
  background: #c82333;
}

/* Upload de arquivo no cadastro de professor */
.file-upload-container {
  margin-bottom: 20px;
}

.file-upload-label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  color: #ffffff;
  font-size: 14px;
}

.file-upload-wrapper {
  position: relative;
  display: inline-block;
  width: 100%;
}

.file-input {
  position: absolute;
  opacity: 0;
  width: 100%;
  height: 100%;
  cursor: pointer;
  z-index: 2;
}

.file-upload-button {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 16px;
  background: rgba(255, 255, 255, 0.1);
  border: 2px dashed rgba(255, 255, 255, 0.3);
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
  width: 100%;
  box-sizing: border-box;
}

.file-upload-button:hover {
  background: rgba(255, 255, 255, 0.15);
  border-color: #ff6b9d;
  transform: translateY(-1px);
}

.upload-icon {
  font-size: 20px;
}

.upload-text {
  color: #ffffff;
  font-weight: 500;
  flex: 1;
  text-align: left;
}

.file-help-text {
  display: block;
  margin-top: 8px;
  color: #b0b0b0;
  font-size: 12px;
  line-height: 1.4;
}

/* Estilo quando arquivo está selecionado */
.file-input:valid + .file-upload-button {
  background: rgba(40, 167, 69, 0.2);
  border-color: #28a745;
}

.file-input:valid + .file-upload-button .upload-text {
  color: #28a745;
  font-weight: 600;
}
/* Estilos para Verificação de Professores - Formal */
.verificacao-professores {
  padding: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.section-header {
  text-align: center;
  margin-bottom: 3rem;
  padding-bottom: 1rem;
  border-bottom: 2px solid var(--primary-color);
}

.section-header h2 {
  color: var(--primary-color);
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.section-subtitle {
  color: var(--text-secondary);
  font-size: 1.1rem;
}

.empty-state {
  text-align: center;
  padding: 4rem 2rem;
  background: var(--card-bg);
  border-radius: 12px;
  border: 2px dashed var(--border-color);
}

.empty-icon {
  font-size: 4rem;
  margin-bottom: 1rem;
}

.empty-state h3 {
  color: var(--text-primary);
  margin-bottom: 0.5rem;
}

.empty-state p {
  color: var(--text-secondary);
}

.professores-grid {
  display: grid;
  gap: 2rem;
  grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
}

.professor-card-formal {
  background: var(--card-bg);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
  border: 1px solid var(--border-color);
  overflow: hidden;
  transition: all 0.3s ease;
}

.professor-card-formal:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 48px rgba(0, 0, 0, 0.15);
}

.card-header {
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  padding: 1.5rem;
  display: flex;
  align-items: center;
  gap: 1rem;
}

.professor-avatar {
  font-size: 3rem;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  width: 80px;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.professor-basic-info {
  flex: 1;
}

.professor-nome {
  color: white;
  font-size: 1.5rem;
  margin: 0 0 0.5rem 0;
  font-weight: 600;
}

.professor-email {
  color: rgba(255, 255, 255, 0.9);
  margin: 0 0 0.5rem 0;
  font-size: 1rem;
}

.status-badge {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 500;
}

.status-badge.pending {
  background: rgba(255, 193, 7, 0.2);
  color: #ffc107;
  border: 1px solid rgba(255, 193, 7, 0.3);
}

.card-body {
  padding: 1.5rem;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
  margin-bottom: 1.5rem;
}

.info-item {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.info-item label {
  font-weight: 600;
  color: var(--text-secondary);
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.info-item span {
  color: var(--text-primary);
  font-size: 1rem;
}

.documento-section {
  margin-top: 1.5rem;
  padding-top: 1.5rem;
  border-top: 1px solid var(--border-color);
}

.documento-section label {
  display: block;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 1rem;
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.documento-preview {
  background: var(--bg-secondary);
  border-radius: 8px;
  padding: 1rem;
  text-align: center;
}

.documento-img {
  max-width: 100%;
  max-height: 200px;
  border-radius: 8px;
  cursor: pointer;
  transition: transform 0.3s ease;
  border: 2px solid var(--border-color);
}

.documento-img:hover {
  transform: scale(1.05);
}

.documento-nome {
  margin: 0.5rem 0;
  color: var(--text-secondary);
  font-size: 0.9rem;
}

.btn-visualizar {
  background: var(--primary-color);
  color: white;
  border: none;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9rem;
  transition: background 0.3s ease;
}

.btn-visualizar:hover {
  background: var(--primary-hover);
}

.card-footer {
  padding: 1.5rem;
  background: var(--bg-secondary);
  border-top: 1px solid var(--border-color);
}

.acoes-formais {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.btn-aprovar-formal,
.btn-rejeitar-formal {
  flex: 1;
  padding: 0.75rem 1.5rem;
  border: none;
  border-radius: 8px;
  font-weight: 600;
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.3s ease;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.btn-aprovar-formal {
  background: #28a745;
  color: white;
}

.btn-aprovar-formal:hover {
  background: #218838;
  transform: translateY(-2px);
}

.btn-rejeitar-formal {
  background: #dc3545;
  color: white;
}

.btn-rejeitar-formal:hover {
  background: #c82333;
  transform: translateY(-2px);
}

/* Modal para visualização de imagem */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 2rem;
}

.modal-content {
  background: var(--card-bg);
  border-radius: 12px;
  max-width: 90vw;
  max-height: 90vh;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 1.5rem;
  background: var(--primary-color);
  color: white;
}

.modal-header h3 {
  margin: 0;
  font-size: 1.2rem;
}

.modal-close {
  background: none;
  border: none;
  color: white;
  font-size: 1.5rem;
  cursor: pointer;
  padding: 0.25rem;
  border-radius: 4px;
  transition: background 0.3s ease;
}

.modal-close:hover {
  background: rgba(255, 255, 255, 0.2);
}

.modal-body {
  padding: 1rem;
  text-align: center;
  max-height: 70vh;
  overflow: auto;
}

.modal-image {
  max-width: 100%;
  max-height: 60vh;
  border-radius: 8px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
}

.modal-footer {
  display: flex;
  gap: 1rem;
  padding: 1rem 1.5rem;
  background: var(--bg-secondary);
  border-top: 1px solid var(--border-color);
}

.btn-download {
  flex: 1;
  background: var(--primary-color);
  color: white;
  border: none;
  padding: 0.75rem 1rem;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 500;
  transition: background 0.3s ease;
}

.btn-download:hover {
  background: var(--primary-hover);
}

.btn-fechar {
  background: var(--text-secondary);
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 500;
  transition: background 0.3s ease;
}

.btn-fechar:hover {
  background: #6c757d;
}

/* Responsividade */
@media (max-width: 768px) {
  .professores-grid {
    grid-template-columns: 1fr;
  }
  
  .card-header {
    flex-direction: column;
    text-align: center;
  }
  
  .info-grid {
    grid-template-columns: 1fr;
  }
  
  .acoes-formais {
    flex-direction: column;
  }
  
  .modal-overlay {
    padding: 1rem;
  }
  
  .modal-footer {
    flex-direction: column;
  }
}